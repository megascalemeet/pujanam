import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ============================================================
  // BRAND COLORS
  // ============================================================

  /// Main brand color
  static const Color primary = Color(0xFF700B10);

  /// Darker version of primary
  static const Color primaryDark = Color(0xFF4D070B);

  /// Lighter version of primary
  static const Color primaryLight = Color(0xFF8F252A);

  /// Very light primary background
  static const Color primaryBackground = Color(0xFFF9EDEE);

  // ============================================================
  // SECONDARY / GOLD
  // ============================================================

  /// Main gold accent
  static const Color gold = Color(0xFFB8860B);

  /// Light gold
  static const Color goldLight = Color(0xFFD4AF37);

  /// Dark gold
  static const Color goldDark = Color(0xFF8C6A00);

  /// Very light gold background
  static const Color goldBackground = Color(0xFFFFF8E1);

  // ============================================================
  // BACKGROUND COLORS
  // ============================================================

  /// Main app background
  static const Color background = Color(0xFFFFFBF7);

  /// White background
  static const Color white = Color(0xFFFFFFFF);

  /// Card background
  static const Color card = Color(0xFFFFFFFF);

  /// Soft cream background
  static const Color cream = Color(0xFFFFF8EF);

  /// Section background
  static const Color sectionBackground = Color(0xFFFFFDF9);

  // ============================================================
  // TEXT COLORS
  // ============================================================

  /// Main heading / title
  static const Color textPrimary = Color(0xFF2B1718);

  /// Normal text
  static const Color textSecondary = Color(0xFF5F4A4B);

  /// Muted text
  static const Color textMuted = Color(0xFF8A7778);

  /// Text on primary color
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  /// Text on gold
  static const Color textOnGold = Color(0xFF2B1A00);

  // ============================================================
  // BORDER / DIVIDER
  // ============================================================

  static const Color border = Color(0xFFE7DADB);

  static const Color borderLight = Color(0xFFF0E8E8);

  static const Color divider = Color(0xFFEDE2E2);

  // ============================================================
  // ICON COLORS
  // ============================================================

  static const Color iconPrimary = Color(0xFF700B10);

  static const Color iconSecondary = Color(0xFFB8860B);

  static const Color iconMuted = Color(0xFF806C6D);

  // ============================================================
  // STATUS COLORS
  // ============================================================

  static const Color success = Color(0xFF2E7D32);

  static const Color successBackground = Color(0xFFEAF6EA);

  static const Color error = Color(0xFFC62828);

  static const Color errorBackground = Color(0xFFFDECEC);

  static const Color warning = Color(0xFFED6C02);

  static const Color warningBackground = Color(0xFFFFF3E6);

  static const Color info = Color(0xFF1565C0);

  static const Color infoBackground = Color(0xFFEAF2FF);

  // ============================================================
  // PRODUCT COLORS
  // ============================================================

  static const Color price = Color(0xFF700B10);

  static const Color oldPrice = Color(0xFF8A7778);

  static const Color discount = Color(0xFF2E7D32);

  static const Color rating = Color(0xFFFFB300);

  // ============================================================
  // SPECIAL / OVERLAY
  // ============================================================

  static const Color shadow = Color(0x1A000000);

  static const Color black = Color(0xFF000000);

  static const Color transparent = Colors.transparent;
}
